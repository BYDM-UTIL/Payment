# שדרוג Payment — מערכת אישית לניהול העסקה ישירה של עובדת זרה

אתה פועל כ־Senior Full-Stack Engineer, מומחה Firebase ואבטחת מידע, וכמעצב מוצר Mobile-First.
עבוד בפועל על הפרויקט הקיים המצורף, Payment.zip. אל תסתפק באפיון, בהמלצות או במסכי דמו: בצע את השינויים בקוד, חבר את התהליכים לנתונים, כתוב בדיקות והכן גרסה עובדת.

## 1. המטרה וההקשר

אני מעסיק פרטי של עובדת זרה בסיעוד, בהעסקה ישירה במסגרת גמלת הביטוח הלאומי. אני צריך כלי אישי ופשוט עבורי ועבור העובדת — לא מערכת לחברת סיעוד ולא מערכת SaaS.

יש שני סוגי משתמשים בלבד: המעסיק והעובדת. לעובדת יש טלפון בלבד, ולכן כל תהליך שלה חייב לעבוד בנייד: כניסה, צפייה בתשלומים, קבלת בקשת חתימה, חתימה באצבע וצפייה באישור החתום.

המערכת כבר קיימת ב־Firebase, אך הממשק גדול ומסורבל. המטרה היא לפשט ולהשלים את הקיים, לא להחליף תשתית ולא למחוק היסטוריה.
מדד ההצלחה: אני פותח את המערכת ומבין מיד כמה צריך לשלם, כמה כבר שולם ומה ממתין לחתימה. העובדת מבינה מיד מה שולם לה ומה היא מתבקשת לאשר.

## 2. התחל מהקוד הקיים

בארכיון יש תיקיית chatgpt. אתר בתוכה את שורש הפרויקט לפי package.json; אל תיצור פרויקט חדש בטעות.

הבסיס הקיים:
React 18, TypeScript, Vite, Tailwind, React Router, Zustand, React Hook Form ו־Zod.
Firebase Authentication, Firestore, Hosting ושירות Storage קיים.
i18next עם he.json, en.json, ru.json; react-signature-canvas; vite-plugin-pwa; כלי PDF ו־Excel.

קבצים מרכזיים לבדיקה:
src/App.tsx
src/components/Layout.tsx
src/features/auth/LoginPage.tsx
src/features/onboarding/CompleteEmployeeProfilePage.tsx
src/features/dashboard/DashboardPage.tsx
src/features/payments/PaymentsPage.tsx
src/features/payments/PaymentModal.tsx
src/features/payments/SignatureModal.tsx
src/features/settings/SettingsPage.tsx
src/services/firebase/auth.service.ts
src/services/firebase/firestore.service.ts
src/services/firebase/storage.service.ts
src/hooks/useAuth.ts
src/utils/calculations.ts
firestore.rules
storage.rules
vite.config.ts

ממצאים מהבדיקה הראשונית שעליך לאמת ולתקן:
- SettingsPage מציג הודעת שמירה אך handleSave אינו שומר הגדרות ב־Firestore.
- בהרשמת עובדת ובהשלמת פרופיל מופיע employerId: user.uid, ולכן נוצר שיוך של העובדת לעצמה במקום למעסיק.
- מנגנון הרשמת מעסיק נשען על VITE_ADMIN_CREATION_PASSWORD ובדיקה בצד הלקוח. הסר את המנגנון; אין להשתמש בו כבקרת הרשאות.
- כללי users מאפשרים למשתמש לשנות את מסמך הפרופיל שלו ללא הגבלת שדות תפקיד ושיוך.
- יש חוסר עקביות בטיפול בתפקידי legacy מסוג admin/viewer; normalizeUserProfile אינו תואם להערה על המרת admin.
- כללי תשלום אינם נועלים את התוכן הכספי לאחר חתימה בפני המעסיק.
- בדיקת חתימה משתמשת ב־changedKeys; נדרשת בדיקה גם של הוספת ומחיקת שדות באמצעות affectedKeys ואימות מבנה מלא.
- קיימת תלות ב־Storage לשמירת חתימות, ושם הקובץ הקבוע signature.png מאפשר דריסה באותו נתיב.
- קיימים סכומי ברירת מחדל ונתוני דמו קשיחים במסכים שונים, ויתרת תשלום מחושבת כך שתשלום יתר נעלם.
- קיימת הגדרת PWA עם הפניות לאייקונים שאינם בארכיון, ו־Service Worker שמגדיר מטמון כללי לבקשות Firestore.
- קיימת פונקציית Audit Log, אך יש לוודא שהיא באמת מחוברת לפעולות העסקיות ולא רק מוגדרת.

הבדיקה הראשונית היא קריאת קוד, לא הוכחה למה שפרוס כרגע ב־Firebase. בדוק את הסביבה הזמינה בלי לשנות Production.

## 3. מגבלות מחייבות

שמור את Firebase Project הקיים, המשתמשים, ה־UID, הנתונים, מזהי העובדות, השנים, החודשים והחתימות הישנות. אל תשנה אזור מסד נתונים או ספק.

כל תהליכי הליבה חייבים לעבוד ב־Firebase Spark ללא חיוב, ללא כרטיס אשראי וללא מעבר ל־Blaze. אל תפעיל שירותים בתשלום או שירותי ניסיון שיפוגו. אל תציג free tier של מסלול בתשלום כאילו הוא מבטיח אפס עלות.

בדוק את התיעוד הרשמי העדכני לפני בחירת שירות: בפרט Storage, Cloud Functions, Authentication והתראות. תכנן מגבלות שימוש והודעות חריגה; אין להבטיח שירות חינמי ללא הגבלת שימוש.

אין להוסיף מערכת חברות, מנויים, סליקה, תפקידי משתמש מיותרים, שרת קבוע או אפליקציה נפרדת בחנויות.
המערכת מתעדת תשלומים; היא אינה מעבירה כסף, מתחברת לבנק או מדווחת לביטוח הלאומי אוטומטית.

## 4. חשבונות, כניסה ושיוך מאובטח

מסך כניסה נקי עם בחירת שפה, דוא"ל וסיסמה, הצגת סיסמה ושחזור סיסמה. השתמש ב־Firebase Auth הקיים. אין תלות ב־SMS בתשלום.

התפקיד נקבע לפי הרשאה מהימנה ולא לפי בחירה במסך, localStorage או פרמטר בכתובת.
שמור את חשבון המעסיק הקיים. Bootstrap של בעל המערכת יתבצע בפעולה מנהלית מאובטחת ומתועדת, לא דרך סיסמת אדמין בקוד הדפדפן. משתמש ללא פרופיל תקין לא יקבל הרשאות ברירת מחדל.

המעסיק יוכל להזמין את העובדת לרשומת העובדת הקיימת. הזמנה צריכה להיות אקראית, מוגבלת בזמן, חד־פעמית וקשורה לנמען הנכון. בפתרון המבוסס על דוא"ל, אמת את הדוא"ל לפני קישור רגיש. אל תסתפק במי שמחזיק בקישור כדי לאפשר שיוך לכל חשבון.

ממש קבלת הזמנה וקישור משתמש־עובדת בצורה אטומית ומאומתת ב־Rules. אין הרשאת רשימת הזמנות ציבורית. המשתמשת לא יכולה לשנות בעצמה role, employerId או employeeId. אין להמציא כתובות דוא"ל פיקטיביות.

הפרד בין UID של המעסיק, UID של העובדת ומזהה הרשומה שלה. החלפת עובדת לא מוחקת עובדת קודמת או מעבירה את היסטוריית התשלומים שלה לעובדת החדשה.

## 5. ממשק פשוט ואישי

למעסיק: לכל היותר ארבעה אזורים ראשיים — בית, תשלומים, מסמכים, הגדרות.
לעובדת: לכל היותר שלושה — הבית שלי, התשלומים שלי, הפרופיל שלי.

מסך הבית של המעסיק מציג ברכה אישית לפי הפרופיל, את העובדת הפעילה ואת החודש הנוכחי. הצג עד ארבעה נתונים: סכום לתשלום החודש, סכום ששולם, יתרה ובקשות חתימה פתוחות. הוסף רשימת משימות קצרה ורק פעולות רלוונטיות: רישום תשלום, שליחת בקשה לחתימה וצפייה באישור.

אם יש עובדת פעילה אחת, בחר אותה אוטומטית. אל תדרוש לבחור עובדת ושנה בכל כניסה. גישה לעובדות קודמות תהיה דרך ארכיון.

במסך הבית של העובדת הדגש את התשלום האחרון ואת הבקשה הממתינה לאישור. כפתור מרכזי: "צפייה בפרטים וחתימה". אין טבלאות ניהול או מידע פנימי של המעסיק.

ברירת המחדל בנייד היא כרטיסים, לא טבלת Excel רחבה. במחשב ניתן לאפשר טבלה כחלופה. אין צורך בגרפים במסך הראשי. פנסיה, זכויות, דוחות מתקדמים והגדרות מפורטות יישארו נגישים, אך לא יתפסו ניווט ראשי נפרד.

## 6. תשלומים בהעסקה ישירה

ברירת המחדל לתקופות חדשות היא העסקה ישירה. הצג את שכר העובדת ואת התשלומים מהמעסיק, בלי שדות חברת סיעוד/תלוש חברה שמבלבלים את התהליך הנוכחי.

שמור תשלומי חברה, payslipPaid ונתוני עבר במקום שבו הם קיימים. אל תאפס אותם ואל תשנה סיכומים היסטוריים. מועד המעבר להעסקה ישירה ייקרא מהנתונים או מהגדרה מפורשת; אל תנחש תאריך.

לכל חודש הצג פירוט זכאות ותשלומים בפועל בנפרד. טופס פשוט יכיל שכר מוסכם, תוספות לפי הצורך, תשלומים ותאריך. פרטים מתקדמים ייפתחו רק לפי צורך.

ממש רשימת תנועות תשלום: סכום, תאריך, אמצעי תשלום, אסמכתה אופציונלית והערה. תמוך בכמה תשלומים באותו חודש, מקדמה ותשלום חלקי, בלי ספירה כפולה מול השדות ההיסטוריים.

הפרד בין:
א. מצב כספי: לא שולם / שולם חלקית / שולם / תשלום יתר.
ב. מצב אישור: לא נשלח / ממתין לחתימה / נחתם / נדרשת בדיקה / בקשה בוטלה או הוחלפה.

חתימה אינה הופכת חודש לשולם; רישום תשלום אינו הופך אותו לחתום. כנה סכום שאינו מאומת "נרשם כשולם על ידי המעסיק", לפי הצורך.

שמור חישובים חדשים באגורות שלמות ופורמט תצוגה בשקלים. הצג תשלום יתר בנפרד; אין למחוק יתרה באמצעות Math.max. קיזוז לחודש הבא יהיה פעולה מפורשת, לא אוטומטית.

אל תניח שדמי כיס הם תוספת לשכר: אפשר להגדיר אם מדובר במקדמה מתוך השכר או בתוספת מוסכמת. הפרד בין ברוטו, נטו, ניכויים וסכום לתשלום רק כאשר יש נתונים שמצדיקים זאת.

הגדרות שכר ותעריפים ייטענו ויישמרו באמת ב־Firestore, עם תוקף לתקופה. שינוי תעריף לא ישכתב חודש חתום. העתקה מחודש קודם תעתיק טיוטת רכיבים בלבד — לא חתימות או תשלומים שבוצעו.

גמלת הביטוח הלאומי, ככל שמנהלים אותה, היא נתון מימון פרטי של המעסיק ולא תשלום נוסף לעובדת. אין לצרפה לשכר ששולם.

שמור יכולות קיימות של פנסיה וזכויות במקום משני. אל תמציא שיעורי חובה, זכאות לחגים או דיני עבודה; פרמטרים כאלה דורשים מקור ותאריך או סימון מפורש כהגדרה ידנית.

## 7. חתימה מרחוק — תהליך שלם

המעסיק רושם תשלום ומפיק בקשת אישור לגרסה מסוימת. העובדת מקבלת קישור מאובטח, נכנסת לחשבונה, רואה את מלוא הפרטים בשפתה וחותמת באצבע.

הקישור הוא Deep Link למסך מוגן, לא הרשאה לצפות במידע ללא התחברות. אחרי התחברות חזור לבקשה המקורית. מנע הפניה לכתובות חיצוניות. קישור שהועבר למשתמש אחר לא חושף דבר.

לפני החתימה הצג באופן ברור: תקופה, סכומים, מועדי תשלום, אמצעי תשלום והסכום המדויק שהעובדת מאשרת שקיבלה. אישור חלקי לא יאשר בטעות את השכר המלא. תמוך בחתימות על תשלומים נוספים באותו חודש בלי לדרוס קודמות.

הוסף תיבת אישור ונוסח פשוט של אישור קבלת הסכום המפורט. אין נוסח ויתור על זכויות ואין הצגה כחתימה מאושרת או כשירות חתימה משפטי מוסמך. אפשר "הסכום אינו נכון / בקשת תיקון" בלי לכפות חתימה.

משטח החתימה יהיה מותאם לאצבע, למסך קטן ול־devicePixelRatio. שינוי גודל או סיבוב מסך לא יאבד חתימה. מנע גלילה רק בתוך המשטח. הוסף ניקוי, ניסיון חוזר ומניעת שליחה כפולה.

שמור snapshot בלתי משתנה של תוכן האישור: נתוני התשלום, הגרסה, השפה, נוסח ההצהרה וגרסתו, זהות המעסיק והעובדת, UID החותמת, חתימה וזמן שרת. אל תסתפק ב־signed=true ובתמונה.

העובדת בלבד רשאית ליצור את אישור הקבלה שלה. המעסיק לא יכול לחתום בשמה. נעילת התוכן והחתימה תיאכף ב־Rules, לא רק בכפתור מושבת.

ממש חתימה כפעולה אטומית שבודקת שהבקשה בתוקף והגרסה לא השתנתה. אם המעסיק שינה תשלום במקביל, החתימה תיכשל באופן ברור ותידרש צפייה בפרטים המעודכנים.

תיקון לאחר חתימה ייצור גרסה/אישור חדש עם סיבה וקישור לקודם. המקור נשמר. Hash יכול לסייע בזיהוי שינוי, אך אינו תחליף לנעילה ואינו הוכחה עצמאית לזהות או לחתימה קריפטוגרפית.

אישור PDF יופק בדפדפן מתוך ה־snapshot החתום, לא מתוך נתונים חיים שהשתנו מאז.

## 8. חתימות וקבצים בלי תלות ב־Storage בתשלום

לליבת Spark תכנן שמירת חתימות קטנות במסמכי Firestore נפרדים ומוגנים, למשל PNG דחוס ומקודד, עם תקרה קשיחה של כ־64 KiB לתוכן המקודד. ודא שהמסמך כולו נשאר הרחק ממגבלת הגודל העדכנית של Firestore.

אל תשמור חתימות בשדות שמגיעים עם כל רשימת החודשים, ואל תאנדקס תוכן בינארי/מקודד.

PDF ייווצר מקומית וניתן יהיה לשמור ולשתף אותו; אין צורך לאחסן כל PDF בענן.
לאסמכתה אפשרית העדף מספר העברה. תמונת אסמכתה תהיה אופציונלית, דחוסה ומוגבלת, במסמך נפרד ובטעינה לפי דרישה. אין להפוך Firestore למחסן PDF/וידאו או לפצל קבצים גדולים לעשרות מסמכים.

שמור הפניות לחתימות ולאסמכתאות הישנות. אם קובצי Storage ישנים אינם נגישים במסלול הנוכחי, דווח על כך ושמור את ההפניה; אל תמחק אותם, אל תסמן אותם כמגובים ואל תפעיל חיוב לצורך שחזור.

## 9. התראות: בסיס חינמי ו־Push אופציונלי אמיתי

חובה לממש מרכז התראות בתוך המערכת, עם קראתי/לא קראתי, קישור למסך הרלוונטי והתראות על בקשת חתימה, חתימה שהושלמה ובקשת תיקון. למעסיק יהיו גם תזכורות לתשלום ולחתימות פתוחות.

הוסף כפתור שיתוף קישור באמצעות תפריט השיתוף של הנייד והעתקת קישור, ואפשר הכנת הודעה בשפת הנמענת לשליחה ידנית ב־WhatsApp. אין להציג זאת כשליחה אוטומטית או להשתמש ב־WhatsApp API בתשלום.

Push כשהאפליקציה סגורה הוא הרחבה רצויה, רק אם ניתן לממש אותה ללא חיוב וללא Blaze:
- בדוק FCM יחד עם Cloudflare Worker במסלול Free, כשרת שליחה מינימלי בלבד. המידע והאימות נשארים ב־Firebase.
- אמת תמחור, מכסות, זמינות ללא אמצעי תשלום ומגבלות CPU עדכניות. בדוק בפועל שהאימות והשליחה מתאימים למכסה.
- שמור סודות שליחה רק בסביבת שרת. אל תכניס Service Account או מפתח פרטי ל־VITE_* או לדפדפן.
- אמת Firebase ID Token, כולל חתימה, תוקף, issuer ו־audience; בדוק גם הרשאת המשתמש, השיוך והאירוע בפועל. אל תסמוך על role, נמען או סכום שנשלחו מהלקוח.
- שרת המחזיק הרשאות מנהל חייב לבצע בדיקות הרשאה בעצמו. CORS אינו תחליף לאימות.
- אם מוסיפים outbox, כתוב את האירוע יחד עם הפעולה העסקית. הגדר מנגנון שיגור וניסיון חוזר אמיתי; אין להניח ש־Firestore מפעיל Worker מעצמו.
- תזכורות אוטומטיות כשהכול סגור דורשות מתזמן זמין. בחר מנגנון חינמי מאומת או סמן אותן כתזכורות בתוך האפליקציה בלבד.
- טפל בכפילויות, הגבלת קצב, טוקנים שפג תוקפם והסרת המכשיר בהתנתקות. "נשלח" אינו "התקבל במכשיר".
- שלח התראות כלליות בלי שכר, דרכון או פרטי בנק במסך הנעילה.
- שלב את שירות ההתראות עם ה־Service Worker הקיים; אל תיצור שני רישומים מתנגשים באותו scope.

ב־iPhone יש להציג הדרכה מותאמת להוספה למסך הבית ולהרשאת התראות, ולבדוק תמיכה בפועל. בקשת הרשאה רק אחרי פעולה יזומה של המשתמש. דחייה או חוסר תמיכה לא יחסמו את האפליקציה.

הליבה חייבת לפעול ללא חשבון נוסף. אם הפעלת Push דורשת גישה שאינה זמינה, הכן קוד והוראות הפעלה קצרות וסמן "לא הוגדר"; אל תדווח שההתראות פעילות ואל תעכב את השלמת הליבה.

## 10. שלוש שפות מלאות

שמור עברית, אנגלית ורוסית. עברית RTL; אנגלית ורוסית LTR.
כל המסכים, הטפסים, שגיאות האימות, ההתראות, האישורים, ההזמנות והמסמכים חייבים להיות מתורגמים.

שפה תישמר לכל משתמש בנפרד. המעסיק יכול לעבוד בעברית והעובדת ברוסית. שינוי שפה לא יאבד טופס פתוח או חתימה.
שמור את נוסח האישור המקורי בשפה שבה נחתם; תרגום לצפייה לא ישנה את המקור.
השתמש ב־Intl לתאריכים ומספרים, במטבע ILS וב־Asia/Jerusalem לתקופות ולתזכורות. טפל נכון בכתובות דוא"ל ומספרים בתוך RTL.
אל תשתמש בתרגום אוטומטי דרך API בתשלום.

## 11. נייד, PWA וביצועים

תמיכה מלאה ב־Safari ב־iPhone וב־Chrome ב־Android, וכן במחשב. תכנן מרוחב 360px, בלי גלילה אופקית, עם מטרות מגע של לפחות 44px, פונט קריא, safe-area וניווט תחתון.

תקן את ה־manifest והוסף את נכסי האייקונים החסרים. התקנה למסך הבית היא אפשרות, לא תנאי לצפייה ולחתימה.

טפל במקלדת הנייד, בחזרה מדפדפן חיצוני, בפקיעת התחברות ובהרשאות. עדכון PWA לא ירענן מסך באמצע חתימה או טופס.

הצג מצבי טעינה, שגיאה, אין נתונים וניתוק רשת. טיוטה יכולה להישמר מקומית באופן מוגבל ובטוח; חתימה או תשלום לא יוצגו כנשמרו לפני אישור השרת. אין להבטיח חתימה אופליין.

אל תשמור תגובות Auth/Firestore במטמון כללי של Service Worker. מטמון PWA מיועד לנכסי האפליקציה. הגדר טיפול מפורש במידע מקומי רגיש, נקה אותו בהתנתקות ומנע זליגה בין חשבונות.

השתמש במנויי Firestore מצומצמים עם cleanup, במקור Auth מרכזי ובטעינת נתונים לפי צורך. הימנע מקריאת כל השנים בכל כניסה, polling תכוף וטעינת חתימות לכל כרטיס. טען ספריות דוחות וגרפים רק כשנדרשות.

## 12. הרשאות, פרטיות ותיעוד

Rules בגישת deny-by-default. המעסיק ניגש רק לתיק שלו; העובדת רק לרשומה ולמידע שהותרו לה. בדוק גם קריאות ישירות ל־SDK וכתובות ששונו ידנית.

אמת בעלות דו־כיוונית ולא רק employeeId בפרופיל. הגבל שדות ב־create וב־update, כולל הוספה ומחיקה. אין שינוי חופשי בתפקיד, בשיוך או בחותמת הזמן.
השתמש ב־serverTimestamp וב־request.time לאירועים הרגישים; אל תציג זמן מכשיר כזמן שרת.

מידע פרטי של המעסיק והערות שאינן מיועדות לעובדת יישמרו במסמכים נפרדים שאינה רשאית לקרוא. הסתרת שדה בממשק אינה הגנה.
אל תדרוש פרטי דרכון ובנק לכניסה אם אינם נחוצים לתהליך. הסר לוגים שמדפיסים פרטים רגישים.

הוסף Audit Log לפעולות העסקיות בפועל. זהות המבצע והזמן יאומתו. לאירועים רגישים, קשר את הפעולה והתיעוד באותה פעולה אטומית ובדוק ב־Rules באמצעות getAfter או מנגנון שקול; אל תאפשר ללקוח לדלג על התיעוד.

תעד שההגנות חלות על משתמשי האפליקציה; אין להציג אותן כהגנה מוחלטת מפני בעל הרשאות ניהול בענן.

בדוק סודות בקוד ובקובצי ההגדרה בלי להדפיס את ערכיהם. הבחֵן בין Firebase Web Config לבין סיסמאות ומפתחות פרטיים. אל תפרסם .env.local, היסטוריית .git, Service Account או קובצי גיבוי רגישים.

## 13. תאימות, מעבר וייצוא

שמור תאימות לנתיבים הקיימים:
users/{uid}
employees/{employeeId}
employees/{employeeId}/years/{year}/monthlyPayments/{month}
employees/{employeeId}/years/{year}/pensionPayments/{month}

הוסף collections/שדות רק כשהם נחוצים, למשל תנועות, גרסאות חתומות, הזמנות והתראות. אל תבנה ארכיטקטורה מסובכת לשני משתמשים.

כתוב migration עם schemaVersion, מצב dry-run, גיבוי, אימות תוצאות ואפשרות חזרה. הוא חייב להיות idempotent. שיוך שגוי או מידע חסר דורשים דוח חריגים, לא ניחוש או המצאת זהות חותמת.

אין להפעיל seed או להכניס נתוני דמו ל־Production. אין למחוק היסטוריה כדי לפשט את הממשק.
שמור קריאה של תאריכי legacy וחתימות ישנות; סמן במפורש כשאין להן מטא־דאטה של המנגנון החדש.

הצע ייצוא חינמי של אישור חודשי חתום, סיכום שנתי ונתונים לגיבוי. שמור את יכולת Excel הקיימת אם היא עובדת; CSV הוא חלופה נוספת, לא סיבה למחוק יכולת תקינה.
גיבוי מקומי צריך לכלול גרסאות וחתימות חדשות, ולא רק URL לקובץ חיצוני. ציין אילו קובצי עבר לא נכללו. טפל בסיכון formula injection בייצוא.

ודא PDF קריא בעברית וברוסית, עם עימוד וחתימה ללא חיתוך. בחר מנגנון מקומי חינמי עם פונטים ברישיון מתאים; אל תסתמך על screenshot של מסך חלקי.

## 14. בדיקות ותנאי קבלה

בצע בדיקות יחידה לחישובים, בדיקות הרשאות ב־Firebase Emulator ובדיקות קצה־לקצה למסלולים העיקריים.
לכל הפחות:
- המעסיק רואה את העובדת הקיימת וההיסטוריה נשמרת.
- קבלת הזמנה מקשרת את העובדת הנכונה ואינה יוצרת מעסיק חדש.
- משתמש לא מורשה לא קורא נתונים ואינו מקדם את עצמו לתפקיד מעסיק.
- שמירת הגדרות נשארת אחרי רענון ומשמשת לחישובים.
- חודש הכולל כמה תשלומים, מקדמה, יתרה או תשלום יתר מחושב נכון.
- נתוני payslipPaid היסטוריים לא נספרים פעמיים.
- בקשת חתימה נפתחת אחרי התחברות מהנייד, רק למשתמשת המיועדת.
- חתימה חלקית מאשרת רק את הסכום ששולם; המעסיק אינו יכול לחתום במקומה.
- שינוי גרסה במקביל, לחיצה כפולה, בקשה שפג תוקפה וקישור שהועבר לאחר אינם שוברים את ההגנות.
- מסמך חתום אינו ניתן לדריסה או למחיקה על ידי משתמשי האפליקציה; תיקון משמר את המקור.
- התנתקות והחלפת משתמש אינן חושפות מטמון של המשתמש הקודם.
- עברית, אנגלית ורוסית תקינות גם בהודעות וב־PDF.
- הליבה עובדת עם Storage ו־Functions לא זמינים.
- חוסר הרשאת Push, ניתוק רשת וכישלון שליחת התראה לא יוצרים הודעת הצלחה שקרית.
- האפליקציה נבדקת במסכי 360/390px ובמחשב, בלי רכיב שנחתך.

אין לשנות בדיקות רק כדי להסתיר כישלון. אל תטען שנבדק iPhone אמיתי, Push בפועל או פריסה אם לא הייתה לך סביבת בדיקה מתאימה.

## 15. אופן העבודה והתוצר

תחילה הצג מיפוי קצר: מה קיים, מה מקולקל ומה תחליף. אחר כך עבור למימוש בפועל באותו פרויקט, בשלבים: הרשאות ושיוך, שמירת נתונים ותשלומים, חתימות, ממשק ושפות, התראות ובדיקות.

אל תשאל שוב פרטים שכבר נמצאים בקוד או בנתונים. בהחלטות עיצוב שגרתיות בחר את הפתרון הפשוט ותעד. אם נדרשת גישה חיצונית שאינה זמינה, המשך בשאר העבודה וספק רק את הפעולות הידניות הנחוצות.

שמור שינויי קוד קיימים של המשתמש. אין למחוק קבצים או לבצע reset גורף. עדכן תלויות רק מסיבה נדרשת ובהתאם לתאימות, לא בשדרוג גורף.

בסיום ספק:
1. קוד עובד ורשימת שינויים מרכזיים.
2. תוצאות אמיתיות של build, lint ובדיקות, עם הפרדה בין עבר, נכשל ולא נבדק.
3. Rules, indexes ו־migration מתועדים.
4. הוראות קצרות לחיבור העובדת ולהתקנה בנייד בשלוש השפות.
5. הוראות הרצה ופריסה מדויקות ל־Hosting, Rules ו־indexes הקיימים, בלי לפרוס שירות בתשלום בטעות.
6. טבלת שירותים: מטרה, מסלול, מכסה, האם נחוץ אשראי ומה קורה בחריגה.
7. סטטוס נפרד ל־Push: פעיל ונבדק / מוכן אך טרם הוגדר / אינו זמין ללא חיוב.
8. צילומי מסך מסביבת הבדיקה ודוח קצר על מגבלות שנותרו.

עבוד תחילה ב־Emulator או בסביבת בדיקה. אל תשנה חיוב, משתמשים אמיתיים, נתוני Production או Rules בפרודקשן ללא אישור מפורש. הכן את הגרסה לפריסה, ואל תטען שהפרויקט חי באינטרנט אם לא בוצעה פריסה.

העדף פחות מסכים ויותר בהירות. המורכבות הדרושה לאבטחה ולשמירת היסטוריה צריכה להיות מאחורי הקלעים, לא על העובדת ולא על המעסיק.


---
## מקורות טכניים רשמיים לבדיקת הדרישות בזמן המימוש

המקורות נבדקו בעת הכנת הפרומפט ב־24 בספטמבר 2026. יש לבדוק שוב בזמן המימוש, כי מסלולים ומגבלות עשויים להשתנות.

- Firebase pricing: https://firebase.google.com/pricing
- Cloud Storage billing requirements: https://firebase.google.com/docs/storage/faqs-storage-changes-announced-sept-2024
- Cloud Functions deployment prerequisites: https://firebase.google.com/docs/functions/get-started
- Firestore quotas and document limits: https://firebase.google.com/docs/firestore/quotas
- Firestore field-level write validation: https://firebase.google.com/docs/firestore/security/rules-fields
- FCM trusted sending environment: https://firebase.google.com/docs/cloud-messaging/server-environment
- Firebase ID-token validation: https://firebase.google.com/docs/auth/admin/verify-id-tokens
- Apple Web Push: https://developer.apple.com/documentation/usernotifications/sending-web-push-notifications-in-web-apps-and-browsers
- Cloudflare Workers pricing: https://developers.cloudflare.com/workers/platform/pricing/
- Cloudflare Workers limits: https://developers.cloudflare.com/workers/platform/limits/
- Vite environment-variable exposure: https://vite.dev/guide/env-and-mode
