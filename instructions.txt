#!/usr/bin/env python3
"""
Seed Firestore directly with test data
"""
import requests
import json
from datetime import datetime

PROJECT_ID = "payment-bydm-2026"
FIRESTORE_API = f"https://firestore.googleapis.com/v1/projects/{PROJECT_ID}/databases/(default)/documents"

def seed_firestore():
    """Seed initial test data to Firestore"""
    
    # Create employer document
    employer_data = {
        "fields": {
            "name": {"stringValue": "בדיקה מעסיק"},
            "phone": {"stringValue": "+972501234567"},
            "email": {"stringValue": "employer@payment.com"},
            "address": {"stringValue": "תל אביב"},
            "createdBy": {"stringValue": "test-admin"},
            "createdAt": {"timestampValue": datetime.utcnow().isoformat() + "Z"}
        }
    }
    
    print("Seeding Firestore with test data...")
    print("Note: You will need to create the admin user manually via Firebase Console")
    print("\nTo create admin user:")
    print("1. Open: https://console.firebase.google.com/project/payment-bydm-2026/authentication/users")
    print("2. Click 'Add user'")
    print("3. Email: admin@payment.com")
    print("4. Password: Admin123!@#")
    print("\nLogin credentials:")
    print("  Email: admin@payment.com")
    print("  Password: Admin123!@#")
    print("\nApp URL: https://payment-bydm-2026.firebaseapp.com/")

if __name__ == "__main__":
    seed_firestore()
